<%= render partial: "pages/navigation" %>
<div class="container-fluid">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'shared/flash_messages' %>
    <%= link_to 'Back to Accounts', root_path %> | <%= link_to 'New Account', new_account_path %>
    <div class="button-list">
      <h1>Credit Cards</h1><br>
      <table id="myTable">
        <thead>
          <tr>
            <th></th>
            <th>Name</th>
            <th>Payment</th>
            <th>Balance</th>
          </tr>
        </thead>
      
        <tbody>
          <% @credit_cards.each do |card| %>
            <tr>
              <td style="text-align:center"><%= "Paid" if card.paid? %></td>
              <td><%= link_to card.name, account_path(card) %></td>
              <td style="text-align:right"><%= number_to_currency(card.minimum_payment.abs) %></td>
              <td style="text-align:right"><%= number_to_currency(card.balance.abs) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>